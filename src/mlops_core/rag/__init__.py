"""Retrieval-augmented generation: the corpus' evaluation set now; its index, its
retriever and the agent that uses them next.

Like the model pipeline, it reads the clean layer from Parquet on disk and never imports
the data pipeline.
"""
